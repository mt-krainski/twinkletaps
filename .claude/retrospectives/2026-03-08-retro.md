# Retrospective — 2026-03-08

**Period:** Mar 1–8, 2026
**Sessions analyzed:** 10 substantive sessions (out of ~20 from the period)
**First retrospective using Claude Code** (previous retro used Cursor IDE)

## What's going well

- **Skill system is mature and heavily used.** 17 skills were audited, deduplicated, and improved this week. Skills load correctly before use. The `/execute`, `/wrap`, `/jira` pipeline works end-to-end.
- **Blocker extraction is consistently correct.** All 4 Jira-fetching sessions correctly used `issuelinks` (not description text).
- **Responsive to corrections.** When corrected (lazy descriptions, third-person voice, approval semantics), the agent fixed immediately without pushback.
- **Clean, targeted edits.** Most sessions showed surgical Edit calls rather than full-file rewrites.
- **Verification improved from last retro.** Test suites run in implementation sessions. The verification-before-completion wiring from Feb 20 retro is working.
- **Skill audit session was excellent.** Read 22 files, made 20+ edits across 15 files, only 1 minor user correction.

## What could be improved

### Theme A: Large Jira JSON output handling (4/10 sessions)
`jira-utils search` returns single-line JSON exceeding Read tool's 25K token limit. Caused 70+ min wasted cycles in one session. Same pattern repeated in 4 sessions (aa35f7e9, 809cc9a7, 89ea1fe1, 1566db04).

### Theme B: TDD still being skipped (1/10 sessions)
Session 23ea62db implemented a redirect without writing a test first.

### Theme C: Guessing CLI flags before checking --help (1/10 sessions)
Session 89ea1fe1 used non-existent `--project` flag on `jira-utils search`.

### Theme D: Monorepo path confusion (1/10 sessions)
Session 23ea62db spent ~7 failed tool calls looking for `src/` at repo root instead of `twinkletaps-app/`.

### Theme E: Shell pipe/cat violations (4/10 sessions)
Multiple sessions used `cat ... | python3` or piped commands, blocked by permission policy AND forbidden in CLAUDE.md.

### Theme F: Unnecessary file creation (1/10 sessions)
Session 1566db04 created `parse_jira_board.py` in project root as workaround, couldn't clean it up.

## Past improvement status (from Feb 20 retro)

| Action | Status |
|--------|--------|
| Wire verification into skill chain | **Helped** — tests run before wrap-up |
| Clarify Supabase migrations in CLAUDE.md | **Helped** — no Prisma confusion |
| Fix finishing-branch kanban timing | **Helped** — no premature `50_merged` moves |
| Add "no guessing" rule | **Partially helped** — down from ~5 to 1 occurrence |
| Simplify addressing-PR-comments | **Helped** — merged with receive-review, no retries |

## Agreed improvements (implemented)

### 1. Always use `--pretty` with `jira-utils search`
Added to `/jira` skill Rules section. Prevents single-line JSON output that exceeds Read tool limits.

### 2. Add monorepo package map to CLAUDE.md
Added Project Structure section listing all packages (twinkletaps-app, agent-utils, jira-utils, ticket-loop, twinkletaps-lamp, cursor-retro).

### 3. Add `--help` before first use rule to Jira skill
Added to `/jira` skill Rules section. Prevents guessing CLI flags.

### 4. Reinforce no-pipe rule with explicit alternative in CLAUDE.md
Added guidance to shell commands rule: re-fetch with `--pretty` and read in chunks instead of piping to python/jq.

## Not implemented (discussed, deferred)

### TDD hard gate in `/execute` skill
Proposed adding an explicit stop-and-check gate before production code. User chose not to implement this round.

### Pipe violation reinforcement in MEMORY.md
User preferred keeping this in CLAUDE.md (project-scoped) rather than MEMORY.md (global). Implemented in CLAUDE.md instead.
